# Notorious Discord Bot

Secure Discord/GMod bridge for Notorious Pill Pack. Secrets belong in environment variables, never in GitHub.

Set the variables from `.env.example`, install with `npm install`, then run `npm start`. GMod can post JSON events to `/gmod/event` using the `x-notorious-secret` header.
